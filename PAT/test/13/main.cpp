#include <bits/stdc++.h>

using namespace std;

const int maxn = 0x3f3f3f3f;
int main() {
    int n,m,a,b;

    double p = maxn;

    scanf("%d %d",&n,&m);
    for(int i = 0;i<n;i++) {
        scanf("%d %d",&a,&b);
        p = min()
    }
    return 0;
}
