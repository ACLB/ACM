#include <bits/stdc++.h>

using namespace std;

int dp[30][30];
char str[30];
int main() {
    int T,n;
    scanf("%d",&T);
    while(T--) {
        scanf("%d",&n);
        scanf("%s",str);
        int len = strlen(str);
        memset(dp,0,sizeof(dp));
        for(int i = 0;i<len;i++) {
            
        }
    }
    return 0;
}
